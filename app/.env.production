# Production environment variables
# These values work when deployed with reverse proxy

# Base path for the application (matches your FastAPI /content prefix)
VITE_BASENAME=/content

# API base URL (includes the content prefix)
VITE_API_BASE=/apis